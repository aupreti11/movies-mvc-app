#this file is used to supply configuration data to spring
#by default tomcat runs on port 8080
server.port=7777

# I am going to use JSP in this project
spring.mvc.view.prefix=/WEB-INF/pages/
spring.mvc.view.suffix=.jsp

#this is for spring boot 2.x
#database configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/rab3techdb?serverTimezone=UTC
spring.datasource.url=jdbc:mysql://localhost:3306/rab3techdb?serverTimezone=UTC

#spring.datasource.url=jdbc:mysql://localhost:3306/rab3techdb?sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'&jdbcCompliantTruncation=false
spring.datasource.username=root
spring.datasource.password=amritupreti
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true